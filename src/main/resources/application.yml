#spring.application.name: demo

app.run.type: default


test:
 goods:
   1:
     goodsNo: "1"
     goodsName: "Product AA"
   2:
     goodsNo: "2"
     goodsName: "Product BB"


spring:
  datasource:
    hostip: ${hostip}
    username: ${username}
    password: ${password}